{
  "name": "resumeai-monorepo",
  "version": "1.0.0",
  "description": "AI-Powered Resume Builder - Full Stack Application",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build:frontend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:all": "npm run build:backend && npm run build:frontend",
    "install:all": "npm install --legacy-peer-deps && cd frontend && npm install --legacy-peer-deps && cd ../backend && npm install --legacy-peer-deps",
    "clean": "rm -rf frontend/node_modules backend/node_modules frontend/.next backend/dist",
    "typecheck": "npm run typecheck --prefix frontend && npm run typecheck --prefix backend",
    "start": "cd frontend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-username/resumeai.git"
  },
  "keywords": [
    "resume",
    "ai",
    "nextjs",
    "express",
    "typescript",
    "tailwind"
  ],
  "author": "Your Name",
  "license": "MIT"
}